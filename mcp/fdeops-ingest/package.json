{
  "name": "fdeops-ingest-mcp",
  "version": "3.10.2",
  "private": true,
  "description": "Thin stdio MCP sink for FDE Fieldbook ingest (stage → propose → apply). Zero runtime dependencies.",
  "bin": {
    "fdeops-ingest-mcp": "./server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
